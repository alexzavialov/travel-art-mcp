{
  "name": "travel-art-mcp",
  "version": "1.6.0",
  "private": true,
  "description": "MCP server for travel.art — exposes art-tourism data (events, museums, itineraries) to AI agents.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.90.0"
  }
}
